{
    "name": "phpnitro/database",
    "description": "PhpNitro database connection factory — Doctrine DBAL-backed, works with SQLite (zero setup), MySQL, and PostgreSQL via a single DATABASE_URL.",
    "type": "library",
    "require": {
        "php": ">=8.1",
        "doctrine/dbal": "^4.4"
    },
    "require-dev": {
        "phpunit/phpunit": "^13.2"
    },
    "autoload": {
        "psr-4": {
            "Engine\\Database\\": "src/"
        }
    }
}
