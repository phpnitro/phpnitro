[package]
name = "phpnitro-render"
version = "0.1.0"
edition = "2021"
publish = false
description = "Shared rasterization/hit-testing core for PhpNitro's native draw-command protocol (Engine\\Native\\Canvas)."

[lib]
name = "phpnitro_render"
crate-type = ["cdylib", "rlib"]

[dependencies]

[dev-dependencies]
