# PhpNitro — identité visuelle

Éclair blanc sur dégradé orange→rouge (`#F97316` → `#DC2626`) : vitesse et puissance ("Nitro"), cohérent avec l'ambition du framework (natif, rapide, PHP comme vrai runtime).

- `logo.svg` — logo maître, vectoriel, 108×108 (mêmes proportions que l'icône adaptative Android).
- Réutilisé tel quel comme icône de l'app de démo (`android/app/src/main/res/drawable/ic_launcher_*.xml`).
