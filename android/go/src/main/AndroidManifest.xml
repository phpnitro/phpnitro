<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- android.permission.INTERNET already comes from :engine (PhpServer
         always needs it there; here it's what lets ConnectActivity/
         NativeRenderPocActivity reach a remote dev server instead). -->

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
        android:usesCleartextTraffic="true">

        <!-- Plain HTTP to a `phpx serve` dev server on the LAN — no TLS in
             this v1 (see the plan's own "hors scope" list), hence
             usesCleartextTraffic above. -->
        <activity
            android:name=".ConnectActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
